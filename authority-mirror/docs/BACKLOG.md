# Backlog

- **Status：** Active Intent / Never Implementation Approval

| ID     | Item                                                 | State                                                 | Dependency / Reason                                                                        |
| ------ | ---------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| BL-001 | 全量怪物与 NPC 库                                          | Deferred                                              | 先验证小型数据结构                                                                                  |
| BL-002 | 全职业与完整角色构筑                                           | Deferred                                              | v0.1.0 仅两名验证角色                                                                             |
| BL-003 | 完整规则搜索系统                                             | Deferred                                              | 与战斗闭环职责分离                                                                                  |
| BL-004 | 完整规则引擎与合法性判断                                         | Deferred                                              | DM 最终裁定权优先                                                                                 |
| BL-005 | 六角格实现                                                | Deferred                                              | v0.1.0 只预留 `gridType`                                                                      |
| BL-006 | 3D、高度体积与垂直范围                                         | Deferred                                              | v0.1.0 高度仅数值提示                                                                             |
| BL-007 | 美术地图、动态边界与复杂地图编辑器                                   | Deferred / Committed Direction                        | 先确定逻辑格数再导入图片；四向热扩展需有符号坐标、可逆边界事件与资源存储合同。                                                   |
| BL-008 | 墙体、碰撞、视线、遮挡、掩护                                       | Deferred                                              | 需要独立几何与规则合同                                                                                |
| BL-009 | 多人实时同步、账户、权限、房间                                      | Deferred                                              | 单 DM 本地闭环优先                                                                                |
| BL-010 | NAS 与正式部署                                            | Deferred                                              | 未进入运行部署阶段                                                                                  |
| BL-011 | 公平性服务器骰子                                             | Deferred                                              | v0.1.0 仅本地事件化骰子                                                                            |
| BL-012 | 复杂动画与特效                                              | Deferred                                              | 非基础闭环                                                                                      |
| BL-013 | 完整召唤物控制系统                                            | Deferred                                              | v0.5.0 已验证受控不死生物的独立实例与关系；通用召唤、权限、AI 和完整规则执行仍未实现。                                              |
| BL-014 | 公共发布、内容授权与商标审计                                       | Blocked                                               | 资料授权、翻译与再分发权 `unknown`                                                                     |
| BL-015 | Archive Contract/Profile 选择                          | Completed through v0.6.0 / Per-delivery Decision      | v0.3.0、v0.3.1、v0.4.0、v0.5.0、v0.6.0 已分别完成选择与 Gate；历史选择不继承。                                |
| BL-016 | 数据 Schema 稳定化与迁移策略                                   | Partially Implemented / Ongoing                       | 已有版本化 Schema、后继 storage key 与 copy-on-write 迁移；未来 Schema 变化仍需独立兼容和回退证明。                              |
| BL-017 | 多战斗 Campaign 与跨设备同步                                  | Deferred                                              | 单会话、本地保存和刷新恢复已验证；多战斗 Campaign、跨设备身份与同步仍未实现。                                                       |
| BL-018 | Character Conversation, Import and Combat Projection | Partially Implemented in v0.3.0 / Remainder Deferred  | v0.3.0 已完成长期角色卡、受控导入与战斗投影；对话式建卡、Agent Gateway 与自动完整建卡继续延期。                                  |
| BL-019 | 战后长期角色结算、永久损伤与永久奖励                                   | Deferred / Later Candidate                            | 独立、可修订的 `CharacterSheet` 已存在；永久损伤/奖励仍需独立效果合同，不得把战斗 HP 或效果直接覆盖到长期数据。                         |
| BL-020 | 0 HP 生命周期、怪物死亡与角色死亡豁免                                | Implemented in v0.4.0 / Archived                      | v0.4.0 已完成并归档 PC `0 HP`、死亡豁免、稳定与死亡垂直闭环；v0.2.0 怪物/NPC DM 记录保持。复活/转化的获批窄流程已在独立 v0.5.0 归档；完整击晕、外围状态/伤害引擎与规则自动裁定仍延期。            |
| BL-021 | 玩家端骰点触发与战斗效果提示框架                                     | Deferred / Later Candidate                            | 依赖多人账户与权限、权威骰点结果、触发候选合同及 DM 确认边界；未进入已归档的 v0.3.0，未来仍需新 ABC。                                  |
| BL-022 | 全职业、多职业升级与合法性引擎                                      | Deferred / Committed Direction                        | v0.3.0 已建立多职业与多施法来源骨架；完整升级、选项资格、全部职业组合与自动合法性判断后续单独交付。                                      |
| BL-023 | 全量法术内容与复杂法术执行引擎                                      | Deferred / Committed Direction                        | v0.3.0 已建立法术档案、准备状态、资源与战斗投影骨架；v0.6.0 仅批准八个非阻塞提示；完整执行引擎继续延期。                                |
| BL-024 | 多规则版本角色兼容与转换                                         | Deferred / Committed Direction                        | v0.3.0 已保留规则来源与人工覆写边界；跨版本自动换算、冲突处理和升级工具仍未实施。                                                   |
| BL-025 | 角色详情术语本地化一致性                                         | Implemented in v0.4.0 / Archived                      | v0.4.0 已完成施法属性展示中文映射与 footer 当前身份修复；稳定内部 ID 保持不变。                                                 |
| BL-026 | 受控生物的法术续控与规则档案                                       | Partially Implemented in v0.6.0 / Archived — Local Private | v0.6.0 已实现“活化死尸”“唤起亡灵”的结构化非阻塞裁定资料与结果记录；不自动校验合法性、时间、材料、法术位、目标上限或升环效果。                    |
| BL-027 | UI 工作台架构、独立页面与可配置布局                                | Implemented in v0.7.0 / Amendment 01 Review Complete | 原冻结范围及 Amendment 01 收口已实施并完成 Review；下一门禁为 User Local Private Release Authorization。 |
| BL-028 | 关联单位与受控生物的战斗中投入                                      | Deferred / Later Candidate                            | 当前仅支持第 0 回合物化；需把独立实例、地图摆放、先攻、主人/控制关系、事件与重复防护接入既有临时投入流程。                                      |
| BL-029 | 单位库搜索、筛选与快速定位                                         | Deferred / Committed Direction                        | 单位模板增长后需要按名称、类型、来源、归档和可投入状态快速收敛候选；查询状态只属于 UI，不得改写 UnitTemplate、战斗事实或事件。                         |

## BL-007 美术地图、动态边界与复杂地图编辑器

### 想解决的问题

未来地图不能继续把网格尺寸、可见容器、图片和棋子位置视为同一个 CSS 背景。DM 应先确定逻辑格数与每格距离，再导入地图图片并对齐；跑团过程中还必须能临时向上、下、左、右扩展空白格，而不移动既有棋子、不拉伸底图或破坏位置历史。

v0.7.0 Amendment 01 只候选修复现有空白网格的 `MapViewport / MapCanvas` 越界，不实施本 Backlog 的图片、动态边界、负坐标、资源存储或事件能力。

### 候选数据模型

- 地图至少区分 `baseBounds` 与 `workingBounds`。两者都使用半开区间的有符号边界：`minX / minY / maxXExclusive / maxYExclusive`。
- `baseBounds` 是创建地图和对齐图片时确认的正式范围；`workingBounds` 是当前会话可摆放、可计算范围，可以大于基础范围。
- 默认空白地图仍从内部 `(0,0)` 开始，保持现有位置数据；向上/左扩展只减小 `minY/minX`，向下/右扩展只增大最大边界。既有单位、范围原点和历史位置不得因扩展而批量平移。
- 用户层默认编号保持正数；扩展到原点之前时显示负数编号，零只作为内部原点。显示编号与距离计算必须分离，不能用展示格式改写 Domain 坐标。
- 地图图片作为 `baseBounds` 下方的独立资源层；Session 只保存资源身份/hash、对齐变换、透明度与边界引用，不应把无界 Base64 图片直接塞入 `localStorage`。

### 地图导入流程

1. 先设置列数、行数与每格距离；未确认逻辑格数时不能把图片作为权威地图。
2. 再选择本地图片，进入不会写入战斗事件的对齐预览。
3. 最低支持缩放、偏移、裁切、90°旋转和网格透明度；默认把图片对齐到 `baseBounds`，但最终结果由 DM 确认。
4. 自动网格识别即使以后加入，也只能作为候选提示；不得未经确认改变格数、坐标或棋子位置。
5. 图片加载失败、资源丢失或格式不支持时保留逻辑网格、单位与事件，显示可恢复错误，不能用空图片覆盖资源身份。

### 四向热扩展与视觉合同

- 地图四边提供低干扰的 `+1格 / +5格` 扩展控制；向上和向左是首版必要能力，不得只实现向右和向下。
- 临时扩展区使用与基础地图有区别但不过度醒目的蓝灰底纹或细斜纹；基础边界以虚线/角标标识，地图图片不延展到新增区域。
- 扩展追加 `map.bounds.expanded` 或等价顺序事件，保存方向、数量、前后边界与操作者语义；撤销通过补偿事件恢复，不删除历史。
- 收缩只能作用于最外层连续空白区域；存在棋子、多格占位、待投入位置、范围预览或其他位置引用时必须阻止并指出对象。
- 适应屏幕、战术尺寸、滚动位置和视口中心属于呈现；`workingBounds` 与扩展/收缩属于 Domain 事实，不能保存在 `UiPreferences`。

### 进入实施前必须决定

- 有符号边界进入哪个后继 Session Schema，以及旧 `width/height`、单位位置、范围原点、待投入草稿和历史事件如何 copy-on-write 规范化；旧键必须保持只读。
- 用户坐标编号、零点表达和坐标输入控件的最终格式；默认正数、负数扩展和旧界面显示必须保持可解释。
- 地图资源使用 IndexedDB、受控 File Handle 或其他本地资源存储的具体方案，以及导出、导入、资源缺失和文件体积上限。
- 最大边界、单次扩展上限与超大地图性能门禁；不得允许无限 DOM 网格导致浏览器失去响应。
- 图片裁切/对齐是否保存原图与非破坏性变换；建议保留原资源身份，只保存变换参数。
- 必须通过新的 ABC 冻结 Schema、事件、迁移、回退、资源失败和浏览器验收；本 Backlog 的 Committed Direction 身份不构成版本安排或 Implementation 授权。

## BL-022 至 BL-024 确定延期方向

以下能力属于产品确定保留的发展方向，但 Backlog 身份不构成具体版本、时间或 Implementation 授权：

- **BL-022：** 覆盖全部职业与子职、逐级升级、职业/种族/背景/专长先决条件、完整多职业组合、法术位贡献和角色构筑合法性；任何自动判断都必须基于已准入且版本明确的规则 Entry，并保留 DM 覆写与审计说明。
- **BL-023：** 建立可审计的法术内容库以及法术目标、射程、范围、组件、持续时间、专注、豁免、攻击、升环和复杂效果的执行合同；自动化失败或规则冲突时必须退回提示与 DM 裁定，不得猜测。
- **BL-024：** 支持同一角色在 2014、2024 或其他明确来源之间进行版本识别、差异预览、选择性转换和可逆迁移；不得把不同版本的同名字段静默合并，也不得把未经核验的内容自动升级。

v0.3.0 已归档并只建立了承载这些方向的稳定数据骨架和最小人工验证路径；不能用“结构已预留”宣称上述三项已经完成。v0.6.0 的八个提示模板也不改变该结论。

## BL-027 UI 工作台架构、独立页面与可配置布局

### 当前 v0.7.0 Approved/Frozen 范围

- User 于 `2026-09-01` 授权生成、批准并冻结 `../version-work/v0.7.0/ABC.md`，随后独立授权并完成 Implementation。User 于 `2026-09-02` 授权生成并随后批准冻结 `../version-work/v0.7.0/AMENDMENT_01.md`，同时明确授权 Implementation；Amendment 已实施完成。User 已确认人工审核通过并授权 Review；Review 已完成。User 随后授权先后执行 Local Private Release 与 Archive；Release 与 Lightweight Archive 均已完成。
- 冻结范围只使用一个主 Slice：只读 Status/Workspace Projection、稳定 Shell 与七个工作区、高密度信息层级、统一视觉系统、三个复杂工作区的受控布局，以及 v0.6.0 copy-on-write/Legacy 对照回退保护。
- 五个 Implementation Phase 仅作为未来施工风险边界，不是 M3 Slice、产品版本、Release 或 Archive。
- BL-028、新 D&D 规则/玩法、外部 UI 框架、多人服务、部署和公开发布均在冻结范围之外。原范围及 Amendment 01 Implementation、自动测试、浏览器复验、Review、Local Private Release 与 Archive 已完成；Commit 与 Push 仍未授权。

### 长期目标与边界

- 将现有 Tab 逐步演进为可独立承载的页面或工作区；允许合适的模块按页面上下文显示、隐藏、排序或调整布局。
- 布局偏好、可见性和面板尺寸必须与角色、战斗、地图及事件日志分开保存；调整界面不得写入团务事实或改变 DM 裁定。
- 不以此建设完整 VTT、多人实时协作或规则自动执行；仍保持 DM 辅助工具定位。

### 版本前审计触发

每次 User 要求开始新版本审计时，必须在候选方向结论中单列本项，并核对：

1. 当前是否新增或继续依赖全量重渲染后的 DOM 注入、跨视图直接查询、模块级临时 UI 状态或重复事件绑定；
2. 现有流程是否已因导航、面板拥挤、信息切换或跨模块操作而妨碍真实 DM 使用；
3. 是否已具备以页面壳层、面板注册表、独立 UI 状态和回归测试为边界的最小重构范围；
4. 结论是继续延期，还是建议 User 将其作为候选版本。建议不构成 Implementation 授权。

### 版本号判定

- **`v0.6.1`：** 仅限视觉修补、文案/层级优化，或经回归证明的内部重构；不得改变既有导航、可操作流程、可持久化偏好或用户可见能力。
- **`v0.7.0`：** 若引入独立页面、可选择显示的模块、布局配置或布局持久化，即使不增加规则内容，也属于新增的用户可操作产品能力，建议使用 Minor 版本。
- User 于 `2026-08-31` 认可上述广义 UI 工作台范围使用 `v0.7.0`，并于 `2026-09-01` 授权生成、批准并冻结 ABC。该冻结不自动授权 Implementation；任何收窄、扩张或版本身份改变都必须通过 Amendment 并由 User 重新批准。

## BL-028 关联单位与受控生物的战斗中投入

### 候选边界

- 允许 DM 在已开始、未结束的战斗中，将长期角色卡中已明确记录的关联单位或有效受控生物，按既有“待入场批次”加入本场战斗。
- 新单位必须仍是独立 `CombatantInstance`；加入前由 DM 设置或确认地图位置、先攻及同值顺序。主人/控制者关联、模板快照、状态、事件历史和重复加入防护必须同时建立。
- 只复用现有 DM 临时投入的确认、地图摆放、先攻与事件边界；不按名称猜测关联、不自动判断召唤/控制合法性、时机、持续时间、命令、资源或规则效果。

### 进入实施前必须决定

- 关联单位或受控生物在本轮的首次行动时点，是否完全复用普通临时加入的先攻规则；建议复用，避免额外行动。
- 主人或控制者已临时离场、死亡或不在本场先攻时，是否允许投入及如何记录关联状态。
- 同一长期关联在同一场战斗中离场、再入场或已经存在历史实例时，如何保证只恢复原实例而非新建重复单位。
- 本项需新的 ABC，不能由 v0.6.0 或已有“第 0 回合物化”能力自动扩展为 Implementation。

## BL-029 单位库搜索、筛选与快速定位

### 想解决的问题

单位库在模板数量较少时可以直接浏览表格；模板增长后，DM 不应依赖长列表滚动寻找单位。User 于 `2026-09-02` 明确将搜索与筛选确认为重要后续开发功能。本项保留为确定方向，但不进入当前 v0.7.0 验收修正。

### 候选边界

- 在单位库标题区建立紧凑工具条，最低支持名称搜索，以及怪物 / NPC、参考 / 自定义 / 变体、已归档和当前阶段可投入状态筛选。
- 始终显示匹配数量；战斗进行中同时显示待入场批次数量，让“查找 → 加入待入场”保持在同一任务上下文。
- 搜索和筛选只缩小当前可见候选，不改变 `UnitTemplate`、`EncounterMember`、`CombatantInstance`、`CombatEvent` 或角色长期数据。
- 查询条件属于 UI 状态；是否跨刷新保存必须在未来 ABC 中决定。即使保存，也不得进入战斗 JSON 导出或生成事件。
- 首版不加入自然语言规则检索、模糊规则解释、外部搜索服务、全文规则索引、自动推荐或自动合法性判断；这些能力分别受 BL-003、BL-004 与规则准入边界约束。

### 进入实施前必须决定

- 中文名称、别名与大小写的最小匹配规则，以及无结果时的清空筛选和新建模板入口。
- 多筛选条件采用交集还是允许同组并集；建议不同维度取交集、同一维度允许多选。
- 已归档模板默认隐藏还是显式显示；建议默认隐藏，并提供一键纳入。
- 搜索条件是否写入独立 `UiPreferences`；建议只保存常用筛选，不保存一次性文本查询。
- 必须通过新的或重新批准的 ABC 冻结交互、UI 状态隔离、窄屏表现、性能和验收标准；Backlog 身份本身不构成 Implementation 授权。

## BL-026 受控生物的法术续控与规则档案

### v0.6.0 已批准边界

User 于 `2026-08-31` 已批准 [v0.6.0 ABC](../version-work/v0.6.0/ABC.md)。该版本只把八种复活、转生与受控不死生物法术组织为非阻塞提示，并让 DM 用一次明确选择记录推荐结果或必要的其他可能性。它不建设完整法术执行引擎。

其中 BL-026 只覆盖“活化死尸”“唤起亡灵”与既有受控关系的逻辑关联：产品可提示常见对象、控制关系与后续裁定，但资格、材料、法术位、施法时机、目标数量、升环、经过时间及结果是否成立均由 DM 决定；信息不全不得阻止记录。

### 后续延期范围

- 若未来需要确定性校验，再逐项准入目标遗体/遗骨、生成 Template、命令距离、控制期限、到期前重施、目标上限与升环变化；不得由 v0.6.0 的提示文本外推。
- 若未来需要精确的 Campaign Time Authority，再单独决定如何表示 24 小时等期限；v0.6.0 不把一次长休自动等同于 24 小时，也不按现实时间推进。
- BL-023 的全量法术内容与复杂执行引擎继续延期；八种法术进入 v0.6.0 不代表 BL-023 获准实施。

### 当前门禁

- v0.6.0 ABC 已 Approved / Frozen；Rules Baseline 已记录八种法术及“遗体防腐”的本地来源与文件 hash。
- User 已授权并完成 v0.6.0 Implementation、Review、Local Private Release 与 Lightweight Archive；自动测试、浏览器完成流程与 User Human Acceptance 均已通过，八项 Archive Gate 均为 Pass。ABC、Backlog 或规则提示条目本身不构成未来交付的授权。
- 任何自动合法性、自动时间、自动资源消耗或完整法术执行能力仍须新的或重新批准的合同。

## BL-021 候选边界

### 想解决的问题

未来允许 PC 玩家在获授权的角色范围内发起攻击、豁免、能力检定或伤害骰，并在最终点数与命中、未命中、重击等结果确认后，立即收到武器精通、偷袭、职业特性、法术、反应或其他战斗效果的候选提示。提示只代表“可能触发”，不把复杂规则解释或对其他单位的修改权直接交给玩家。

### 候选事件流程

```text
roll.requested
→ roll.resolved
→ outcome.confirmed
→ trigger.candidates-generated
→ trigger.proposed
→ trigger.applied / trigger.rejected
```

- 触发判断使用已确认的最终结果，不只读取裸 `d20`；必须保留能力修正、熟练、加值、优势 / 劣势与其他修正的审计输入。
- 同一结果可能产生多个候选，玩家或 DM 可逐项确认；应用后写入顺序事件，并以补偿事件撤销，禁止重复应用。
- 玩家只能操作明确授权的角色。涉及其他单位、隐藏信息、地图状态或 DM 专属裁定的效果，必须由 DM 或预先批准的房间策略确认。
- 动画、音效、提示高亮属于结果呈现层；即使呈现失败，也不得改变骰点与战斗事件事实。
- 断线、重连、重复提交与多端同时确认时必须保持幂等，不能重复消耗资源或重复结算伤害。

### 进入实施前必须满足

- 依赖 BL-009 的账户、房间、角色归属和权限边界，以及 BL-011 的权威骰点方案；复杂动画可再依赖 BL-012，但不是触发合同成立的前提。
- 需要稳定的骰点结果、效果候选、资源消耗、目标选择、确认与拒绝 Schema；规则证据不足时只能提示 DM 手工裁定。
- 必须决定哪些候选可由玩家直接确认、哪些必须经 DM 确认，以及客户端预测和权威状态冲突时的处理方式。
- 本项未进入已归档的 v0.3.0；进入任何未来交付前必须通过新的 ABC 冻结权限、事件语义、规则准入、失败恢复与验收标准。

## BL-020 已实施边界

### 想解决的问题

让本地战斗会话能够把“临时离场”和“死亡”严格区分：怪物或 NPC 的 HP 降至 `0` 后可按 DM 选择进入死亡状态；角色在未即时死亡时可进入 `0 HP`、昏迷、死亡豁免、稳定或死亡等明确状态。该项不把伤害裁定、复活或全部状态规则扩展为完整规则引擎。

`v0.2.0` 先采用最窄的怪物/NPC 表示层：`dead` 与 `temporarily-away` 分离、死亡后退出先攻并保留尸体棋子、DM 特许 1 HP 复起，以及保留原死亡实例的新实例特殊转化。`v0.4.0` 随后完成并归档 PC `0 HP`、死亡豁免、稳定与死亡的垂直闭环，因此 BL-020 的批准范围已完成。

### 已完成与继续延期

- v0.4.0 已基于独立 Rules Baseline Entry 实现 `active / dying / stable / dead / needs-review`、死亡豁免计数与轨迹、0 HP 伤害、治疗、医疗稳定、回合约束、顺序事件、补偿撤销和 copy-on-write 迁移。
- `dead`、`temporarily-away`、昏迷与倒地保持分离；死亡单位的后续复活/转化由已归档 v0.5.0 的明确 DM 事件处理。
- 完整非致命击晕、昏迷外围效果自动执行、稳定后 `1d4` 小时自动恢复、完整伤害引擎与规则自动裁定继续延期。

冻结合同、Implementation、Testing、Review、Release 与 Archive 证据均保存在 `version-work/v0.4.0/`；本活文档不改写这些历史记录。

## BL-019 候选边界

### 想解决的问题

战斗中的普通 HP 损失、临时 HP、Buff、Debuff 和行动状态默认都属于单场 `CombatantInstance`，不应在“结束战斗”时被一个笼统的回写按钮自动变成长期角色变化。DM 应能逐角色明确选择：

1. `结束战斗，无永久损伤`：长期当前 HP、HP 上限和永久效果保持战前值；
2. `结束战斗，获得永久损伤`：经二级菜单和再次确认，写入永久 HP 上限扣除及一个或多个永久 Debuff；
3. `结束战斗，获得永久奖励`：经二级菜单和再次确认，写入永久 HP 上限增加及一个或多个永久祝福 / Buff。

### 候选数据与交互

- 复用现有独立于 `CombatantInstance`、`UnitTemplate` 和事件日志的长期 `CharacterSheet`，再为永久效果与结算历史建立独立合同；不得改写既有卡片身份与修订边界。
- 永久 Debuff / Buff 至少包含名称、自定义说明、来源战斗、创建时间、状态和可选骰式；解除或修正必须追加补偿记录，不得无痕删除历史。
- 永久 HP 变化和可选效果数值使用可追加骰项的构造器，例如 `[2]d[8] + [1]d[4] + [3]`，标准化为 `2d8+1d4+3`；允许继续添加 `[数量]d[骰面]` 项和末尾固定修正值。
- 每个数值效果必须明确选择求值模式：`roll-once`（获得时掷一次并固定结果）或 `roll-on-trigger`（保留公式，每次在战斗中触发时重新掷骰）。两种模式都保存原始公式；前者保存固定逐骰结果和合计，后者逐次记录触发时间、逐骰结果、合计、应用目标和对应战斗事件。
- 骰式只生成 DM 指定的数值，不自动判断伤势、祝福、规则合法性或效果语义，不扩展为完整规则引擎。
- 战斗数据选择“不保存”时，完整 `CombatSession`、地图和事件日志是否丢弃，与已由 DM 明确确认的长期角色变化必须分开处理；默认候选是丢弃战斗记录但保留长期结算及其最小审计来源。

### 与战斗系统联动

- 长期诅咒 / 祝福保存在 `CharacterSheet.persistentEffects`，角色加入遭遇时只投影为本场 `CombatantInstance` 的只读来源快照；战斗内触发记录不得偷偷反写或改写长期效果定义。
- “战斗”Tab 为当前角色增加“长期效果”区域，始终显示名称、正负类型、骰式、求值模式、固定值或本次可触发状态，以及长期角色卡来源。
- `roll-once` 效果在获得时确定数值；进入后续战斗时沿用同一固定值。例如永久 HP 上限 `-2d8` 在获得时掷出 `-11`，以后每场战斗都按 `-11` 投影，不再重掷。
- `roll-on-trigger` 效果保留类似 `2d8+1d4+3` 的公式；在“战斗”Tab 由 DM 点击“触发并掷骰”，每次产生新的结果和顺序事件。首版不自动判断“每回合”“命中时”或“受伤时”等复杂触发条件。
- 数值必须声明应用方式，候选最小集合为：伤害、治疗、临时 HP、本次掷骰修正、当前战斗资源变化和仅记录数值。HP 上限的永久增减只允许 `roll-once`，避免每次触发重新改写长期角色结构。
- 每次动态触发先显示本次结果和目标，DM 再确认应用；取消则只保留预览、不写事件。确认后追加例如 `persistent-effect.rolled` 与相应战斗变更事件，仍可通过补偿事件撤销本次战斗应用。
- 解除、压制或永久修改诅咒 / 祝福必须回到长期角色卡并经 DM 确认；在战斗 Tab 隐藏或暂时不触发，不等于删除长期效果。

### 进入实施前必须决定

- “无永久损伤”是保持完整战前长期状态，还是允许单独回写当前 HP / 资源；建议默认完整保持战前状态。
- 永久 HP 上限变化是否最低保持为 `1`；建议默认最低为 `1`，任何例外另行二次确认。
- 每个 Debuff / Buff 选择 `roll-once` 或 `roll-on-trigger`；两者都应实现。建议 HP 上限变化只允许 `roll-once`，战斗伤害、治疗、临时 HP、掷骰修正和自定义数值允许两种模式。
- 动态效果的触发频率是否需要自动化；建议首版仅支持 DM 在“战斗”Tab 手动点击触发，自动每回合 / 命中 / 受伤触发延后到有独立规则合同时再决定。
- 选择“不保存战斗”后是否仍保留已确认的长期角色结算；建议保留，否则“永久”无法成立。
- 本项进入任何版本前，必须通过新的或重新批准的 ABC 定义长期角色卡 Schema、迁移、预览、撤销和验收标准；不得附带进入 v0.2.0。

## BL-018 候选边界

### 想解决的玩家问题

新手不该先被要求理解职业、背景、专长、熟练项、法术位或完整法术表，才能开始创建角色。玩家可以从“我想玩一个优雅、神秘、平时低调、关键时刻很厉害的旅行者”这样的角色幻想开始；系统的职责是帮助他逐步收敛，而不是把几个职业名轮流硬推给他。

玩家应能保存“喜欢 / 不喜欢 / 尚未决定”的偏好，拒绝推荐并继续修正。系统最后才把概念落实为职业、背景、技能、装备、法术等规则字段；任何自动采用的默认项都必须可见、可逐项修改。默认采用最新适用规则，但必须显示规则版本，并允许玩家明确选择旧版或其他来源。

### 预期体验

背景是可选共创，不是强制写作：玩家可以只要规则字段，或选择一句动机、轻量人物关系，或在同意后再扩展完整经历与战役钩子。玩家可以明确说“不想写背景”“太沉重”或“不要家族仇恨”。叙事背景和规则背景效果可分别调整；仅面向 DM 的钩子、禁忌主题和关系摘要必须与玩家卡分开保存，且经玩家同意后才可共享。

施法职业必须有独立流程：以“伤害、控制、防御、治疗、探索、社交”等实际玩法组织推荐，解释玩起来的感觉，并展示专注、准备数量、法术位、已知法术或法术书等关键约束。新手可选预设备法术包，高级玩家仍可细调；不能把完整法术表直接扔给新手。

### 交接到战斗

目标是让自然对话只能生成可拒绝、可修正的 `Character Draft`，再按以下流程进入 Terminal：

```text
Agent 生成 Character Draft
→ Terminal 解析与 Schema 校验
→ 显示导入预览、缺失项、默认项与规则版本
→ 玩家 / DM 确认
→ 写入长期 Character Sheet
→ 生成可进入战斗的 UnitTemplate / CombatantInstance
```

Agent 不能直接改写已有长期角色卡、战斗实例或事件日志；导入失败也不得覆盖已有数据。长期角色卡、战斗实例和事件日志仍分离；战斗结束后的 HP、资源和法术位继续经过 DM 审核回写。每份角色卡需要版本、修订与规则来源状态，不能把模型自然语言当作唯一规则事实。

### Agent 是可选入口

Agent 只负责对话、解释与生成 Draft；Terminal 的手工建卡、JSON 导入和战斗功能不能依赖联网模型。API Key 不得放进浏览器。属性修正、熟练项、法术位、字段完整性与 Schema 校验优先由确定性代码完成；复杂施法、多职业或规则冲突才考虑升级模型能力。

每次调用应可追踪模型、Token、费用和检索的最小规则范围，并支持每角色、每团预算上限和关闭 Agent 模式。不得反复发送完整规则库，也不得在 Terminal 内直接建设大型 RAG、Embedding 或向量数据库。

### 首版不做什么

不包含全量规则引擎、无人审核的“角色绝对合法”承诺、自动裁定复杂法术、多人实时协作、公共开放规则库、自动公开分享角色或背景、向浏览器暴露 API Key，或把 LoreBuddy 全部资料直接嵌入 Terminal。

### 未来首版的接受方向

新手应能自然描述角色幻想、拒绝和修正推荐；完成一个非施法角色与一个施法角色；得到可读简介与结构化草案；在 Terminal 预览、校验、确认并导入；导入后直接创建战斗实例；战斗结束仍走 DM 审核回写。关闭 Agent 后，Terminal 核心功能必须不受影响；成本、模型调用和规则范围必须可追踪。

在其进入新交付前，优先解决可验证角色数据进入战斗的基础能力；对话式建卡、施法向导、成本控制和 Agent Gateway 应作为后续独立范围再经 ABC 批准。

Backlog 项只能通过新的或经重新批准的 ABC 进入 Implementation。
