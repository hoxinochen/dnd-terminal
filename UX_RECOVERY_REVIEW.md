# v0.7.0 候选分支 UI 恢复与适配记录

日期：2026-09-10。范围：当前候选工作区，保留 Gemini/AG 已有改动；没有提交、推送、合并或修改产品版本。

## 本轮结果

- 宽屏三列：全量 20/55/25，战斗 20/42/38，死亡处理 20/39/41。地图模式独占可用宽度。侧栏收起后使用 44px 轨道，其余空间归地图；窄容器改为纵向任务流。
- 死亡处理临时展开右侧，不覆盖平常侧栏偏好。允许手动收起；修正收起后仍保留大块空列的问题。
- 地图提示条支持鼠标、单指拖动和方向键平移。棋子移动仍使用原有拖动提交逻辑。
- 地图空白短点按进入地图模式，再点返回来源模式；没有来源时回战斗。棋子、控件、拖动及范围/入场/当前死亡处理草稿不触发这一导航。
- 浅色改灰白层次、羊皮纸保持褐色纸面；生命卡和死亡/特殊状态标签使用主题语义颜色，取消态势行整行淡化。修正生命卡溢出、窄栏表单、复选框及顶部骰子栏布局。
- 保留资源操作后的展开状态与动作详情展示，不重写死亡、后继、资源或战斗结算规则。

## 可复现验证

1. `node --test tests/*.test.mjs`：26 个测试文件通过。
2. `node --check src/app.js`、`src/battle-workbench.js`、`src/map-controller.js`：通过。
3. `git diff --check`：通过。
4. 本地候选服务运行于 `http://127.0.0.1:4174/` 时执行 `node scratch/verify-ux-recovery.mjs`。需要 macOS Chrome 和支持原生 WebSocket 的 Node；使用独立临时 Chrome 配置，不读取或修改用户浏览器存档。

Chrome 隔离浏览器已通过：320、390、768、1024、1280、1440 六档宽度 × 三主题，无页面横向溢出、骰子控件越界；死亡标签对比度不低于 4.5:1；三列尺寸、收起轨道、鼠标/触控平移、地图返回导航、资源折叠状态、后继继承选项保留均通过；平移和空白导航前后战斗存储字节一致；控制台错误为 0。

截图输出：`/tmp/dnd-ux-review/`。已人工查看三主题桌面及窄屏截图。此轮不是 Safari/Firefox 实机验收，也不是所有死亡分支的逐项人工重演；已有领域测试保持通过。
